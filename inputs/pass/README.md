# PASS deck inputs

Upload `.ppt` and `.pptx` files directly into this folder. Do not upload a ZIP.

Use these exact workflow paths:

- `inputs/pass/ch-08-clotting-system-jun-2022.ppt`
- `inputs/pass/ch-09-catabolic-pathways-jun-2022.ppt`

Run one deck per workflow run. Leave `deck_url` blank when using a repo path.
